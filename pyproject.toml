[build-system]
requires = ["setuptools>=77.0", "setuptools_scm>=8.1"]
build-backend = "setuptools.build_meta"

[project]
name = "stacie"
authors = [
    { name = "Gözdenur Toraman", email = "gozdenur.toraman@ugent.be" },
    { name = "Toon Verstraelen", email = "toon.verstraelen@ugent.be" },
]
description = "STACIE is the STable AutoCorrelation Integral Estimator for time-correlated data."
readme = "README.md"
license = { text = "LGPL-3.0-or-later OR CC-BY-SA-4.0 (for documentation)" }
requires-python = ">=3.11"
keywords = [
    # snipwise.md BEGIN keywords
    "ACF",
    "autocorrelation function",
    "autocorrelation integral",
    "characteristic timescales",
    "data analysis",
    "exponential correlation time",
    "integrated correlation time",
    "molecular dynamics",
    "open source",
    "post-processing",
    "power spectral distribution",
    "PSD",
    "Python package",
    "scientific computing",
    "STACIE",
    "time correlation",
    "transport properties",
    "uncertainty quantification",
    # snipwise.md END keywords
]
classifiers = [
    "License :: OSI Approved :: GNU Lesser General Public License v3 or later (LGPLv3+)",
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "attrs>=23.1.0",
    "matplotlib>=3.9.0",
    "numpy>=2.0.2", # Due to https://github.com/numpy/numpy/issues/27036
    "scipy>=1.13.0",
]
dynamic = ["version"]

[project.optional-dependencies]
docs = [
    "furo",
    "intersphinx-registry",
    "jupyter",
    "jupyter-cache",
    "jupytext",
    "mdtraj",
    "myst-nb",
    "myst-parser",
    "nglview",
    "numpydoc",
    "openmm>=8.0",
    "packaging",
    "pandas",
    "setuptools_scm",
    "sphinx",
    "sphinx-autobuild",
    "sphinx-autodoc-typehints",
    "sphinx-codeautolink",
    "sphinx-copybutton",
    "sphinx-sitemap",
    "sphinxcontrib-bibtex",
    "sphinxcontrib-svg2pdfconverter",
    "sphinxext-opengraph",
    "stepup>=3.1.2",
    "stepup-reprep>=3.1.5",
]
tests = [
    "numdifftools>=0.10.1",
    "pytest>=8.2.0",
    "pytest-regtest>=2.1.1",
    "pytest-timeout>=2.3.1",
    "pytest-xdist>=3.6.1",
]
dev = [
    "stacie[docs,tests]",
    "pre-commit",
    "snipwise<2.0.0a0",
]

[project.urls]
Documentation = "https://molmod.github.io/stacie/"
Issues = "https://github.com/molmod/stacie/issues/"
Source = "https://github.com/molmod/stacie/"

[tool.pytest.ini_options]
addopts = "-n auto --dist worksteal -W error"
timeout = 120

[tool.setuptools_scm]
version_scheme = "post-release"
local_scheme = "no-local-version"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
    "A", "B", "BLE", "C4", "E", "EXE", "F", "I", "ICN", "ISC", "N", "NPY", "PERF", "PIE",
    "PL", "PT", "PYI", "RET", "RSE", "RUF", "SIM", "SLF", "TRY", "UP", "W"
]
ignore = [
    "ISC001",   # https://docs.astral.sh/ruff/rules/single-line-implicit-string-concatenation/
    "PLC0415",  # https://docs.astral.sh/ruff/rules/import-outside-top-level/
    "PLR0904",  # https://docs.astral.sh/ruff/rules/too-many-public-methods/
    "PLR0911",  # https://docs.astral.sh/ruff/rules/too-many-return-statements/
    "PLR0912",  # https://docs.astral.sh/ruff/rules/too-many-branches/
    "PLR0913",  # https://docs.astral.sh/ruff/rules/too-many-arguments/
    "PLR0914",  # https://docs.astral.sh/ruff/rules/too-many-locals/
    "PLR0915",  # https://docs.astral.sh/ruff/rules/too-many-statements/
    "PLR0916",  # https://docs.astral.sh/ruff/rules/too-many-boolean-expressions/
    "PLR0917",  # https://docs.astral.sh/ruff/rules/too-many-positional/
    "PLR2004",  # https://docs.astral.sh/ruff/rules/magic-value-comparison/
    "PT011",    # https://docs.astral.sh/ruff/rules/pytest-raises-too-broad/
    "RUF001",   # https://docs.astral.sh/ruff/rules/ambiguous-unicode-character-string/
    "TRY003",   # https://docs.astral.sh/ruff/rules/raise-vanilla-args/
]

[tool.pymarkdown]
# Report unknown or mistyped settings instead of silently ignoring them.
mode.strict-config = true
# Rule md007 (ul-indent) expects a list nested in an ordered list item
# to be indented to the parent's content column, i.e. three spaces for a `1. ` item.
# MyST only nests such a list when it is indented four spaces.
plugins.md007.enabled = false
plugins.md013.line_length = 100
plugins.md013.heading_line_length = 100
plugins.md013.code_block_line_length = 100
# Rule md022 (blanks-around-headings) counts a MyST anchor, such as `(v1.2.1)=`,
# as content that must be separated from the heading it labels,
# while MyST only attaches the anchor to the heading on the line right below it.
plugins.md022.enabled = false
# The changelog repeats headings such as "Changed" under every release,
# which is fine as long as they do not repeat under the same release.
plugins.md024.siblings_only = true
# The default list does not include the elements of the picture in the README.
plugins.md033.allowed_elements = "!--,![CDATA[,!DOCTYPE,p,picture,source,img"
# Rule md041 (first-line-heading) does not fit `docs/data/zenodo.md`,
# which is the description of an archive rather than a page with a title.
plugins.md041.enabled = false
